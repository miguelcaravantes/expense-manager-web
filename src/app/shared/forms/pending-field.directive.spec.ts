import { PendingFieldDirective } from './pending-field.directive';

describe('PendingFieldDirective', () => {
  it('should create an instance', () => {
    const directive = new PendingFieldDirective(null);
    expect(directive).toBeTruthy();
  });
});
